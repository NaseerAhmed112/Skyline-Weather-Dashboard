{
  "name": "skyline-weather-backend",
  "version": "1.0.0",
  "description": "Skyline Weather Dashboard - File-based backend (Express + JSON file storage) that proxies Open-Meteo geocoding & forecast APIs and persists favorite locations to a JSON file.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
